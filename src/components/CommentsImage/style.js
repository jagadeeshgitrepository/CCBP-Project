import styled from 'styled-components'

export const CommentImage = styled.img`
  width: ${props => (props.tableComment ? '17px' : '25%')};
  margin-right: 3px;
  margin-top: 3px;
  position: relative;
  font-family: 'HK Grotesk';
  font-size: 30px;
  font-weight: normal;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.5;
  letter-spacing: normal;
`

export const Commen = styled.img``
