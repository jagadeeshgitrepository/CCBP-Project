import styled from 'styled-components'

export const CommentImage = styled.img`
  width: ${props => (props.tableComment ? '25px' : '40%')};
  margin-right: 8px;
  position: relative;
`

export const Commen = styled.img``
